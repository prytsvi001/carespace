{
  "name": "teamspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server", "extension"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "db:migrate": "npm run db:migrate --workspace=server",
    "db:seed": "npm run db:seed --workspace=server",
    "db:studio": "npm run db:studio --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "build:extension": "cd extension && vite build",
    "vercel-build": "node scripts/prepare-vercel-schema.js && npx prisma generate --schema=./prisma/schema.prisma && npm run build --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
